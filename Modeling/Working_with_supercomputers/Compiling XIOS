##### Compile the trunk version
Checkout the `svn` repository of XIOS. 
```
svn co [http://forge.ipsl.jussieu.fr/ioserver/svn/XIOS/branchs/xios-2.5](http://forge.ipsl.jussieu.fr/ioserver/svn/XIOS/branchs/xios-2.5)
```
##### Compile the 2.5 version
```
svn co http://forge.ipsl.jussieu.fr/ioserver/svn/XIOS/trunk
```

### Compile on IRENE
```
./make_xios --prod --arch X64_IRENE_GCC --full --job 8
```

### Compile on Jeanzay
```
./make_xios --prod --arch X64_JEANZAY --full --job 8
```
